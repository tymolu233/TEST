name: Build

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Install dependencies
      run: sudo apt-get install gcc-aarch64-linux-gnu

    - name: Build
      run: aarch64-linux-gnu-gcc -o chall chall.S
